# Local image assets

This repository avoids committing binary images to keep PRs accepted by the repo policy.

To use the real portrait on the home page, add this file locally:

- `public/images/ali-alsherif.jpg`

The UI ships with a text-based SVG placeholder instead.
